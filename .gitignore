# Python runtime
.venv/
.venv-workers/
__pycache__/
*.pyc

# Node / Cloudflare
node_modules/
.wrangler/

# Build artifacts (should NOT be in repo)
python_modules/
pylock.toml
uv.lock

# Secrets — NEVER commit
.env
.env.*
*.token
*.credential
access.jsonl

# Cloudflare / 本地凭证 —— HANDOFF 记录 Cloudflare API Token 存在这些位置
.dev.vars
.dev.vars.*
.git-credentials
wrangler.toml.local
*.pem
*.key

# 测试产物
.pytest_cache/
tests/fixtures/__pycache__/
bench/cache/
bench/build.log
bench/run.log
bench/access_log.json
