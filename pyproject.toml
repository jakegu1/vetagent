[project]
name = "vetagent"
version = "0.1.0"
description = "VetAgent — token risk intelligence MCP for AI agents"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
