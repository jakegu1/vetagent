# Work backlog (BACKLOG.md)

> The queue. One line per item, a stable id, and **how it will be verified**.
> `tests/test_backlog.py` enforces the shape; `docs/ROUNDS.md` records what actually got
> done and when.

## The rule that keeps this from becoming a wishlist

**Every open item names a verification.** A command, a test, or a measurement that will
say whether it worked. An item that cannot name one is not a task, it is a hope, and it
gets deleted rather than carried.

This is the same standard the rest of the repository is held to, and it exists because
three plausible-sounding features were killed by measurement in a single day: LP-burn
detection (fired on 58% of good tokens), reserve-event backfill (unaffordable on free
RPCs), and using market activity to settle sellability (traded away the one attack the
check exists to catch). None would have been caught by a plan.

**Ids are permanent.** A closed item keeps its number and its outcome, including
`Rejected`, because "we looked at this and decided not to" is the most useful thing a
backlog can tell the next person. `Done` items name the round that did them.

**What does not belong here**: parked *ideas* that would change direction (→
`OPPORTUNITIES.md`, which has review dates and a test), decisions already taken (→
`DECISIONS.md`), and anything the scorecard already enumerates as a gap — link to it
instead of restating it.

| State | Meaning |
|---|---|
| `Open` | Queued. Must have an owner and a verification. |
| `Blocked` | Cannot start; the blocker is named. |
| `Done Rn` | Finished in round n. |
| `Rejected` | Measured and dropped. The evidence stays. |

---

## Mine

| # | Item | Why it matters | Verify | State |
|---|---|---|---|---|
| W2 | Deployer history as a risk dimension | One of two dimensions still uncovered on the scorecard | `python bench/scorecard.py` shows 10/11 dimensions covered, and the new signal separates dead from alive by >10pp on `bench/results.json`. Needs a contract-creation source first; Etherscan-family APIs are keyed | Open |
| W3 | Grow the genuine adversarial cohort beyond 17 | Every headline claim rests on it, and the rewritten launch post now leads with a figure computed on 17 tokens | **Measured 2026-09-09, and the measurement corrected the plan.** This row said the lever was "more recent days, or a deeper sample per day". Yield by source says something narrower: **snapshot 7/104 = 6.7%, backfill 9/285 = 3.2%, head 1/187 = 0.5%**. The archive is twice backfill and thirteen times the head pages, and it is the only source that compounds. It also *underfilled* its 30% quota — 104 of an allowed 173 — which looked like a depth problem and was not: the archive was three days old the day the dataset was built, and now holds **4,631 distinct base tokens** over seven days, 26x the quota it could not fill. Quotas rebalanced backfill 0.40→0.25, snapshot 0.30→0.45, taking the shift out of backfill rather than head, because head is the control group the false-positive rate is measured against and B8 exists because truncation once starved it. At the new mix ~23 more adversarial tokens needs roughly 345 snapshot rows, well inside one day of archive. **Deliberately not rebuilt yet:** a rebuild moves every published number, and the launch post is written against the 2026-09-07 snapshot with the gate nine days out. Rebuild after the post, not under it | Open |
| W4 | Cut the unknown rate below 10% | 17.9%, worth 4 scorecard points | **Blocked** on W5. Measured 2026-09-06 after the chainID fix: of 100 unknowns, **1** is our side — down from 62. 57 are the simulator having no record of the token and 42 are its buy leg reverting. Both are one vendor's limits, and the remaining engineering on our side is done | Blocked |
| W6 | EVM holder concentration | The last uncovered dimension | **Blocked** by `DECISIONS.md` B2 — the field belongs to the held-out oracle. Needs W1's bytecode route or a different source | Blocked |
| W7 | Point-in-time evaluation from the snapshot archive | The benchmark scores "now" against a retrospective label, so it cannot answer "would it have warned me". The archive records contemporaneous state and the labeller never reads it | **Blocked** on archive depth: needs ≥60 days of contemporaneous snapshots, 3 as of R11. Accrues on its own via `snapshot.yml`, so the wait is the work. **Acceptance criteria, adopted 2026-09-14 from the ChatGPT strategy evaluation §5/§8.1:** warning lead time (minutes from first actionable alert to the adverse event, the central number); pre-event recall (share of known events flagged before they happened -- replaces dead recall, which is post-event by construction); false-block rate (legitimate rows rated medium or high); unknown rate split into infrastructure and coverage; age of the critical evidence at decision time. Incident replay and counterfactual checks are deliverables of this item, not separate ones. **Two constraints the evaluation did not price, both measured:** no 5/15/30/60-minute event snapshots -- the Worker's shared egress already draws 33.8% `unknown` from rate-limited upstreams (60 minutes, 2026-09-12), so event cadence would push the live service into `unknown` for everyone; keep 4x/day and say in the report that lead time is bounded by cadence. And input evidence may be stored for pools already in the public archive, never per query | Blocked |
| W17 | **Score** owner powers, rather than only disclosing them | Blocked twice over: the cohort is 9, and the instrument finds 31% of what it looks for | **One of the two blockers is gone.** The instrument was the reason given in R12 and it no longer is: W18 took pooled recall to 62.5%, and the tax power to 89.5%. What blocks scoring now is the cohort and the precision, and both are numbers rather than intuitions. The oracle confirms 34 of our 44 tax claims and 15 of 27 blacklist claims, but only **10 of 47** pause claims -- and that last figure is partly an artefact rather than an error, because `can halt trading` split out of pause and the oracle has no field for a trading gate, so 43 contracts naming one are compared against a question it was never asked. Still **blocked on W3**: 17 adversarial tokens cannot support a threshold, and E19 stands -- disclose, do not score | Blocked |
| W18 | A selector source that is not the labelling oracle | The bytecode scan finds 31% of the powers that exist because contracts name these functions in more ways than a fixed list holds. Deriving the list from the oracle's labels would fix it and void the benchmark | **Done R19.** Three steps, none of which reads a label: every PUSH4 immediate in the 559 cached contracts (2,230 distinct selectors, median 21 per contract), resolved through the public directory at openchain.xyz (1,940 named, 290 it holds no entry for, **0 unasked**), then classified by a rule three independent drafts wrote from Solidity naming before any recall was measured. Pooled recall against the oracle's own flags **41.4% -> 62.5%**; the tax power, the worst at 7.9%, is now 89.5%; blacklist 26.3% -> 78.9%. The row asked for ~80% before any discrimination claim means anything: two powers clear it, mint at 55.1% and pause at 52.6% do not. 23 pinned selectors became 285. **The control did its job**: recall on a held-out half of the contracts matched or beat the half the rule could have been influenced by, on all four flags, so the rule was not fitted. Four admission gates, each watched go red on a planted input -- recomputation through keccak, the ERC-20 surface refused by selector (the directory answers `40c10f19` with a ground collision, and `ffffffff` with `LOCK8605463013()`), a prevalence ceiling that removes nothing today and says so, and the name rule itself. Cross-checked by a second PUSH4 extractor written independently: identical corpus figures, and it measured the three gaps that remain -- leading-zero selectors (0.31%), selectors shifted into a PUSH32 (largest, but all 19 EQ sites are outside their dispatcher), and jump-table dispatchers, which are **unmeasured rather than zero** **Corrected 2026-09-12 (R20-1): every figure in this row is IN-SAMPLE.** The list was mined from the same 559 contracts it is scored on, and 193 of its 285 selectors occur in exactly one of them. Run the identical pipeline over half the corpus and score it on the other half -- which is what a contract nobody has seen looks like -- and pooled recall is **50.0%**, not 62.5%, with the tax power at **31.6%**, not 89.5%. The dev/holdout split cited below holds out LABELS, not BYTECODE: it can catch a name rule fitted to the oracle's answers, and correctly found none. The rule was controlled; the universe was not. Both figures are generated into `bench/owner_powers.json`. | Done R19 |
| W21 | Bring R19's outward numbers under the number guard | The guard checked that guarded figures still *match* and never that it *covered* anything, so coverage was whatever somebody remembered to add | **Done R19.** Verified exactly as this row specified — mutated all 87 percentages on the live surfaces, one at a time. Two holes. `LIVE_CLAIM_FILES` was the six files that existed the day it was written, so five surfaces R19 created were never scanned. And every figure inside a markdown blockquote was invisible to it, because `>` is both a blockquote marker and a comparison operator and the guard skips comparisons on purpose — which left the X/Mastodon draft, the most-copied version of the launch post, completely unguarded. 13 surfaces scanned now, plus a stated `GENERATED_FILES` category for files whose guard is regeneration rather than a regex. `tests/test_number_coverage.py` fails if a new surface appears uncovered | Done R19 |
| W22 | Declare `find_new_hot_pools`'s output shape | It was the only tool with no `outputSchema`, so `count`'s meaning — changed 2026-09-08 from "fetched" to "returned" — and `scanned`, `sources_ok`, `sources_failed` were announced nowhere a caller looks. This tool has already shipped two defects of exactly that shape: `count` said 20 beside three pools, and `scanned` could not tell a half scan from a complete one | **Done R19.** All three tools declare one. The test asks whether the schema covers what the tool *returns today*, not whether a schema exists: it drives `new_pools` with a stub and diffs the actual keys against the declared ones, top level and per pool row. Watched it catch a planted undeclared field. `count` and `sources_failed` must also say what they are NOT, since each has already been misread once | Done R19 |
| W23 | The owner page's self-report guards check shape, not substance | `tests/test_owner_page.py` required a CORRECTIONS entry inside 14 days and a cost-of-waiting line per item, and nothing checked that either *said* anything — a dated one-word entry passed. And the maturity guard's only source, `docs/SCORECARD.md`, was itself generated and unguarded | **Done R19.** The scorecard half was not hypothetical: the committed file said `test_risk.py 272 passed` against a real 277, `test_mcp.py 73` against 80, and `6 of 180 days` of archive against 7. Every copy of the total agreed with a number the generator no longer computed, and it still rounded to 55 — luck, not design. `tests/test_published_numbers.py` now regenerates the scorecard and fails on a diff, the check `test_rounds.py` has applied to `ROUNDS.md` for weeks. For the self-reports, a floor: each correction must state a claim, a correction and a mechanism, they must differ, the correction must point at something checkable, no two may repeat, the mechanism may not be "noticed", and the list must be newest-first; each cost line must be a sentence and must not echo its item's title. It caught a real weak entry on its first run. **What it cannot do is stated in the test rather than implied: no check can tell whether a correction is true, or whether the important one was left out** | Done R19 |
| W24 | Normalise the EVM address before it becomes a cache key | `_EVM_RE` accepts `[a-fA-F0-9]`, `validate_address` did not lowercase, and `_fetch_json` keys its cache on the raw upstream URL. So one token had up to 2^40 spellings that upstreams treat as identical and our cache treated as distinct, each miss costing 2-4 upstream calls against an endpoint with **no rate limiter anywhere in `src/`** that we advertise as free and unlimited | **Done R19.** EVM addresses are lowercased at validation; Solana is untouched, because base58 is case-significant. Red first, and it printed the exploit: three spellings produced **6 calls, 6 distinct URLs**; now 2. The row said a benchmark re-run was needed before shipping. It is not, and that is measured rather than assumed: all **576** EVM addresses in `bench/results.json` are already lowercase, so the bytes sent upstream for the benchmark corpus are unchanged, and the live API returned an identical verdict and score for the checksummed and lowercase forms of the same token on 2026-09-09 | Done R19 |
| W26 | Rewrite the Experiment C post against its hostile review | The review found the five headline percentages reproduce exactly, then eleven HIGH findings the two FATALs did not cover (every vendor figure is that vendor own, checked 2026-09-09) | **Done R19.** All four drafts rewritten. The post now leads with the number its title promises and never printed -- 64.7% high on the adversarial cohort, 17.6% ablated, n=17 -- names Forta, ChainAware and the two arXiv head-to-heads as counterexamples to its own retracted claim, shows the ablated column beside every liquidity-coupled figure, states the false-positive population, retracts circular, discloses zero Solana rows, and dates the figures. 27 guarded patterns in the post; a mutation test changes each one in turn and the build catches 27 of 27 | Done R19 |
| W28 | Give `find_new_hot_pools` a `status`, **after 2026-09-18 and not before** | It is the only one of three tools that returns no `risk_level` and no `status`, so `_record_call` writes an empty verdict for it (src/entry.py:728) and the usage gate's "received a real verdict" clause is unsatisfiable by it. Two clients that named themselves plainly-not-ours and came back on three separate days -- `sasame-mcp-audit` (15 calls) and `rokmcp-collector` (3) -- are filed under `no:` for that reason alone. It is also a real product gap: the tool already reports `sources_failed`, so a caller cannot tell a complete scan from a partial one without reading two fields | **The date is the point, not the change.** Shipping this before the gate would alter the gate's own inputs in the direction of YES, which is indistinguishable from moving the bar however good the reason. Decided now, deferred on purpose, and `bench/usage.py` prints the blind spot in the meantime so the 09-18 reading is made with it visible. Verify: the tool returns a `status` that distinguishes a complete scan from a partial one, `test_mcp.py` pins it in the outputSchema, and the first usage run after it ships shows pool-discovery callers carrying a verdict | Open |
| W27 | Does a fresh deploy hand the first callers `unknown`? | **n=2, and that is the whole point of writing it down rather than acting on it.** The two live calls made in the seconds after the 2026-09-10 deploy both returned `unknown`; the six after that returned `low`, and the same address returns `low` consistently now. Two observations is not a result -- an upstream blip explains it just as well as a cold Worker -- but the unknown rate is a scorecard line worth 4 points (W4, at 17.9% and blocked on a credential), and a cold-start contribution would be a lever on it that is **not** blocked on anything. Ruled out already: it is not `verbose`-dependent, and it is not the W18 widening, which only adds an info signal and cannot move a verdict | **The query exists and the field is already collected.** `_record_call` writes `verdict` as an Analytics Engine blob beside method, tool, caller and country, so the unknown rate is measurable per minute without logging anything new -- no address, no identity, nothing the privacy policy forbids. Join those counts against deploy completion times from `gh run list --workflow=deploy.yml` and compare the first five minutes after each deploy with the steady state. Needs `CLOUDFLARE_API_TOKEN`, which W9 already put in GitHub Secrets, so this runs in CI and not on a laptop. **Settled either way by one query**: a cold-start effect shows as an unknown spike aligned to deploy times across several deploys, and its absence across those same deploys retires the hypothesis rather than leaving it open | Open |
| W8 | Consolidate `DECISIONS.md` | 55 rows against its own stated ceiling of 40. The file says test-enforced entries that never failed should collapse to one line | **Done R19, at 44 rows rather than 40, and the shortfall is arithmetic rather than effort.** 56 -> 44: thirteen rows collapsed into `C1`, which keeps every id, its one-line rule and its test, so `test_every_named_test_exists` still checks all thirteen and a reader grepping for `M4` still lands on it. Why not 40: of 27 test-enforced rows, **10 are cited by id elsewhere in the repo** -- `B2` 23 times, `E11` 20 -- and collapsing those leaves dangling references in code that points at them; of the 17 that remain, 4 name a test pinning one branch of a broader rule and must stay. **The rule's justification was false for a majority**: "the test is the documentation" held for 5 of 17. For 8 the reason was moved into the test docstring first and the row deleted after; `test_tools_list_shape` enforced two decisions and had no docstring at all; and `E5` named a test that **could not fail** -- `_score` replaced by the naive sum E5 forbids passed 4 of 4, and again with the 30-point cap removed. Fixed with a test that fails 4 checks against the sum and 3 against a missing cap. Two rows, `B3` and `B4`, said `None` and were enforced all along, verified by deleting their sentences and watching the build go red. The retirement rule now reads "check the test carries the reason, then collapse" | Done R19 |
| W30 | Fix the false and stale public claims the 2026-09-15 numbers audit found | The landing page's structured data says USDT and WBTC are among the 24.0% of centralised assets flagged high; every USDT row and WBTC are `low`. `EXPERIMENT_C.md` says USDT is medium on Ethereum and WBTC medium; both are `low` now. "Half the dead cohort still holds over $5,000" is 14 of 30, and nothing guards it because it has no percent sign. `src/landing.html` names the oracle, which B2 keeps out of `src/`, and the guard never saw it because it scans only `src/*.py` | **Done 2026-09-15 (707f2a7).** `tests/test_published_numbers.py` guards "14 of 30"; the oracle-name guard in `tests/test_risk.py` covers every file under `src/` and is watched red on `landing.html` before the fix | Done R23 |
| W31 | A honeypot.is retry answer that is an error must not become a honeypot | When the first answer is BUY_FAILED the engine retries with `pair=`, and for 8 benchmark tokens (Punk, TRAC, MAI, PIEVERSE, GWEI, UAI, ROBO, COLLECT) the retry answered "Target contract does not contain code" with a 100% sell tax and `simulationSuccess` true. `_sim_failed` reads only `simulationSuccess`, so an upstream error became a fatal honeypot. Replayed on the cache 2026-09-15 (576 of 576 verdicts reproduced): false positives 7 -> 5 of 162, false blocks 31 -> 30 of 154, unknown 105 -> 113 of 576, no unsafe or dead token moves | **Done 2026-09-15.** Benchmark re-run: exactly the 8 predicted tokens moved to `unknown`; false positives 7 -> 5 of 162, false blocks 31 -> 30, unknown 105 -> 113. Red-first test in `tests/test_risk.py` on a fixture carrying that reason; `python bench/run_benchmark.py` re-run with the before/after table in the commit | Done R23 |
| W32 | Fallback pools get the independent-depth check (E21) | CoinGecko and GeckoTerminal pools carry no reserve amounts, so `_independent_depth_cap` calls them not checkable and credits the stated depth in full: a synthetic pool quoted in a non-anchor token at $12.4M was credited $12.4M (verifier replay, 2026-09-15). Since D8 that path serves every token DexScreener does not list, which is where new tokens start. `_gt_to_pair` has known the quote token's address since eb0f41a, so the anchor side can now be checked | **Done 2026-09-15.** Before the fix a token DexScreener does not list, whose only pool was quoted in a coin its creator minted, came back `low` (red test). Benchmark re-run: 9 tokens medium -> unknown, each credited $3k-$25k on a fallback pool with no anchor side; none reached or left `low`; unknown 113 -> 122. Red test first in `tests/test_risk.py`: a fallback pool quoted in a non-anchor is not credited, one quoted in WETH is; `python bench/run_benchmark.py` re-run with the before/after table | Done R23 |
| W33 | "Every pool is empty" must not ignore a deep pool it could not price | In `assess()` pools without an anchor side drop out of the stated list, so a $0 WETH pool beside a VIRTUAL pool stating $115,299 reads "every pool reports empty" and returns a fatal `drained`. Synthetic only: 0 of the 15 benchmark `drained` rows hit it. W37 depends on the same question being asked correctly | **Done 2026-09-15.** Red test: the $0 WETH pool beside a $115,299 VIRTUAL pool read fatal `drained`, high. Benchmark re-run: 0 rows changed verdict, score, driver or gap. Red-first synthetic test; `tests/test_risk.py` green | Done R23 |
| W34 | Say what honeypot.is's honeypot flag is made of | 53 of the 54 benchmark flags where honeypot.is's own simulation succeeded rest on its `holderAnalysis`: real holders whose sells failed, which is what a blacklist honeypot looks like. The engine discards that object, its signal text calls the case "more likely a simulator false positive", and the test fixture flags a token with 0 of 1,555 holders failed -- a shape no real flagged answer in the cache has. Verdicts do not change here; that is W44 | **Done 2026-09-15.** Evidence carries `holder_analysis` when the token is flagged or holders failed; the contested, contradicted and fatal sentences name the holder numbers and no longer say "simulator false positive" or "simulation confirms it" for a flag the simulation did not raise. Benchmark re-run: 0 rows changed. Evidence carries holders, failed and siphoned; the signal names them; the fixture carries a real holder-failure shape; `tests/test_risk.py` green | Done R23 |
| W35 | Record why an answer was `unknown`, without recording the token | Production answered `unknown` to 187 of 460 calls in the week to 2026-09-14 (40.7%) against 18.2% in the benchmark, and the telemetry keeps only method, tool, verdict, client and country. Every proposed fix -- keys, longer caching of honeypot.is -- is therefore argued rather than measured | **Done 2026-09-15.** Blob 6 carries `_why_unknown` (fixed vocabulary, e.g. `infrastructure|liquidity:dexscreener 429`); `bench/usage.py` maps it and `--write-verdicts` adds `unknown_why`. The first reading that can split production unknowns is the first `production.yml` run a week after deploy (W36). One Analytics Engine blob holding `unknown_kind` and the failing upstream's name and status, no address; mapped in `bench/usage.py`; a test red before the change | Done R23 |
| W36 | Does the production probe's daily schedule fire? | `production.yml` has run once, by hand on 2026-09-14. Its first scheduled slot, 2026-09-15 06:40 UTC, had produced no run by 11:00 UTC. One miss is not a broken schedule -- GitHub delays and drops cron runs under load, and the snapshot job's 02:23 slot started at 07:48 the same day -- but the scorecard's production line goes stale 7 days after its last reading (D7), on 2026-09-21 | **Done 2026-09-15: it fires.** The 06:40 UTC slot ran at 12:06 UTC (GitHub delayed it five and a half hours) and committed b5eea54: 189 of 475 unknown in the week to 09-15 (39.8%), a window that is almost all before the CoinGecko key. `gh run list --workflow=production.yml` shows a `schedule` run by 2026-09-17; if not, add a second daily slot and re-check | Done R23 |
| W37 | A pool holding under a dollar is drained, asked of every pool | 31 tokens whose pools state under $1 read `medium` ("very low liquidity") while E18 says the verdict must match the sentence. The audit's version scores only the best pool and would condemn INSTACLAW, which holds $115,299 in a VIRTUAL pool. Replayed: adversarial rated high 10 -> 16 of 17 and centralised 43 -> 66 of 179, both from treating corpses harder and not from detecting hostility, which is how it must be published | **Blocked** on W33 and on the 2026-09-18 gate, since it moves published numbers. Then: condemn only when every in-scope pool that states a depth states under $1; red tests first; benchmark re-run; the commit says what moved the adversarial row | Blocked |
| W38 | A lone lifecycle warning stops forcing `medium` | "Any warn is medium" came over from the additive model in f9a8da0 with no recorded reason, while the weights rate lifecycle 0.4. Replayed on the cache: 22 flips, false blocks 31 -> 25 of 154, and NEMESIS (dead, $84k still in the pool) is the only token that moves the wrong way. "Very little trading" keeps forcing medium: its sentence says exiting at size may be difficult, and a `low` says sellable and liquid (E18) | **Blocked** on the 2026-09-18 gate. Then: rule pre-registered in DECISIONS before the re-run; red-first lifecycle-only fixture; `_CATEGORY_WEIGHT.get(category, 0.5)`, because the literal lookup raises on `price_disagreement`; E14 adversarial review; benchmark re-run; the unguarded "most land at medium" sentence fixed with it | Blocked |
| W39 | Measure before adding a second pool lookup when DexScreener's pools are thin | The audit credits 6 of 7 false positives to pool loading. The replay: the proposed trigger reaches 3 of them and can be shown to clear none, and none of the 80 rows it would fire on has a cached fallback answer, so its effect is unmeasured. Merging fallback pools before W32 would credit depth E21 refuses | **Blocked** on W32. Then: bar fixed before fetching; pool listings for the 80 trigger rows fetched into a separate cache; count tokens that gain an anchor-backed pool of $1,000 or more and which verdicts would move; build only if the bar passes | Blocked |
| W40 | Label new pools while they still hold money | 15 of the 17 adversarial tokens hold under $1, and B11's 7-day-volume guard passed 14 of those 15: a label taken days after launch can only find corpses. The archive records new pools at birth (937 a day first seen on all chains, 12-day mean), so the labeller can ask at T+24h instead | **Blocked** on the Experiment C post, per W3's ordering. Then: a one-day quota probe (about 316 oracle calls, 429s counted); a labeller-side workflow for tokens holding $5,000 or more at T+24h, storing reserve, timestamp and an engine answer taken at the same moment; readout after 30 days, bar fixed now: at least 18 unsafe tokens that held $5,000 or more when labelled | Blocked |
| W41 | Own sell simulation: a design page and a prototype outside the engine | 86 of the 105 benchmark unknowns are sellability gaps that only a second simulator can close. All three RPC hosts the engine uses honoured an `eth_call` code override on 2026-09-15 (balance override and a real router trade not yet tried). The audit's adapter list leaves out Uniswap V4, which holds 22 of the 86 | **Run 1, 2026-09-15: inconclusive under the pre-registered rule** (`bench/sim_probe/v2-2026-09-15.json`): 12 of 13 PancakeSwap V2 unknowns got a definite answer (all sold), 50 of 51 clean controls agree, 10 of 13 honeypot controls agree -- under the 80% bar. All three misses are drained pools (under $0.31), none a honeypot let through. Amendment 1 requires the pool to hold $1,000 or more on the day; 4 candidates exist, fewer than 5, so the positive control is not measurable and V3 waits for W40's cohort. **Design page and rule committed 2026-09-15 before any run: `docs/SELL_SIMULATION.md`.** `eth_simulateV1` with a balance override works on all three hosts, so no tester contract or compiler is needed; the controls now include 16 archived Ethereum honeypots, so agreement is tested in both directions. One page naming V4 and how the prototype is measured; then a script outside `src/` with its own cache: a PancakeSwap V2 tester on BSC against the 13 V2 unknowns plus 20 cleanly answered controls. Rule fixed before running: a definite answer on at least 10 of 13 and agreement on at least 18 of 20 -> Aerodrome next; under 7 of 13 -> stop. No engine integration before the 2026-09-18 gate | Open |
| W42 | Publish the liquidity breakdown beside the rows it explains | "24.0% of centralised tokens rated high" reads as "penalises USDT", which is `low`. By the engine's best pool, the 179 split: no figure 20 (19 high), under $1 40 (17), $1-$1k 15 (1), $1k-$100k 38 (4), $100k or more 66 (2). Tokens holding $100k or more: low 55.5%, unknown 21.1%, medium or high 23.5% of 247, ablated low 64.8%. All five buckets, next to the unchanged 24.0% -- not the two that flatter it | **Done 2026-09-15** on /method, "By the depth of the pool". The figures in the Why cell predate W31/W32; published today: six rows, not five -- an E14 review found 15 of the 21 "no depth figure" tokens had every pool report $0, an observed absence, now its own row -- each with high, medium, unknown and high on contract signals only (B3). 31 of the 40 centralised highs are on pools reporting $0 or under $1, and 30 of those 31 are not high on contract signals. The 246 tokens holding $100k or more: 137 low, 53 unknown, 56 medium or high; on contract signals 159 / 53 / 34, the 22 movers all pool-age or lifecycle driven, 30 of 56 honeypot. The draft's line calling this "closer to what a caller meets" was cut as unmeasured. 48 guarded figures, generated from one row list; watched red. New figures and `TARGETS` in `bench/publish_numbers.py`, watched red by adding the copy before the targets; `tests/test_published_numbers.py` green | Done R23 |
| W43 | Re-measure VIRTUAL under the unchanged $10M anchor rule, and pin the rule | The audit proposes anchoring VIRTUAL from pools adding up to $5.92M. E21's bar was set before measuring and refused VIRTUAL at $8.5M on 2026-09-14; the cached floor is $9.26M. Admitting it clears 2 unknowns. A two-hop rule clears 12 only with a bar low enough to admit ADS, which the audit itself keeps out | **Done 2026-09-15** (`bench/anchor_admission/2026-09-15.json`). The rule's "hold" was pinned before the run -- stated AND credited above $10M. VIRTUAL: $8,341,435 stated, $6,787,349 credited, 29 anchored pools of 30 returned (a floor) -- refused, the bar unmoved. ADS $0.63M, NVDAB $3.38M, FDUSD on BSC $0.24M also refused; all 9 admitted anchors still clear it (lowest wstETH $12.2M). GeckoTerminal cross-check answered 429 for 6 of 13 and decides nothing. `test_the_anchor_table_admits_only_by_its_rule` pins the figures, the refusals and the latest re-measurement; watched red with VIRTUAL added. Live anchored depth measured under the same rule, added only at $10,000,000 or more; a test in `tests/test_risk.py` pinning the admission rule and the refusal list | Done R23 |
| W46 | A honeypot claim from a simulation that never ran must not end as `low` | E14 review of W31 (616d932): a first honeypot.is answer of the never-ran shape now starts the `&pair=` retry, and a clean retry replaces it -- measured in a harness, a quiet pool went high -> low and a busy one unknown -> low. The only W31 path toward `low`; 0 of 1,111 cached first answers and 0 of 434 archived first probes have that shape, so nothing has taken it. Also unpinned: the evidence scrub (reverting it leaves the suite green) | **Done 2026-09-15.** Red: the never-ran first answer plus a clean retry came back `low`, and an empty router with a different reason lost its honeypot claim. Benchmark re-run: 0 rows changed. Red-first `tests/test_risk.py` case: a never-ran first answer saying `isHoneypot` plus a clean retry is not `low`; the classification also requires the "does not contain code" reason (54 of 54); scrub assertions watched red | Done R23 |
| W47 | Does zero gas with a named router mean a honeypot verdict or a trade that never ran? | The same review found 19 cached answers with a router, buy and sell gas 0, `isHoneypot` true and buy-side revert reasons (INSUFFICIENT_OUTPUT_AMOUNT, TRANSFER_FROM_FAILED, STF), three of them on tokens with $948k-$1.76M pools and 2,112-4,787 sells a day. One reviewer read them as real verdicts, another as trades that never executed; neither checked an outcome | **Partly answered 2026-09-15 by W41's run 1:** in 7 archived honeypot.is answers with zero gas and a named router, our own simulation bought normally and the sell reverted (`TRANSFER_FROM_FAILED`) -- so zero gas does not mean nothing executed. Still open: whether the 19 cached zero-gas answers on liquid tokens (NVDAc, GOOGLc, Basecat) are real. Rule fixed before measuring; `assess` replayed offline on the 9 tokens' cached bodies; outcomes checked for the 3 liquid ones; W31's classification changed only if no `unsafe` or `dead` token would reach `low` | Open |
| W48 | The deploy's flood check failed once with no limiter change | `deploy.yml` for 16201b8 (W34, which touches only honeypot wording) sent 75 invalid calls in 28 seconds on 2026-09-15 12:02 UTC and got no 429; the next deploy, 8e268e9, six minutes later, got one. The limiter is a counter in the edge cache, which is per data centre, so a runner whose requests land on two colos could split the count -- believed, not measured. A check that fails at random teaches people to re-run it, and a real regression then gets re-run too | **Colo split measured 2026-09-18** (deploy run 35380924967, 99c4691): one runner's flood landed in two data centres, 74 calls in EWR and 21 in IAD, and the 429 came at call 95, not 61. The limiter counts per IP per colo, so its real ceiling for a client spread across colos is a multiple of 60/min -- this, plus the minute boundary, explains the red deploys. The 125-call check passes anyway; whether to count globally (Durable Object or KV) is a design decision, not a flake. **2026-09-18: 3 of 17 deploys, not 2 of 3** (the ops review read every deploy log since the counter went live). A fixed 60 s window explains two: a 75-call flood across a minute boundary splits 30/49; modelled at 8-26% per deploy with a working counter. The third (ccba688) ran inside one minute and is not explained. The flood is now 125 calls, which at the measured pace crosses at most one boundary, and every call's colo is printed. Still open: whether the third was a colo split. **Recurred 2026-09-15 16:40 UTC** on 29e8c9c (W42, which touches only /method copy): 75 calls, no 429 -- 2 failures in the last 3 deploys that ran the check. The code had been deployed before the smoke test, and /method was verified live. Two of three is no longer a flake to watch; the colo measurement is the next step. Record the colo (`cf-ray` suffix) of each flood call in `deploy.yml`; count failures over the next 10 deploys; if any recur, the colo split is confirmed or refuted from those rows before the threshold or the check changes | Open |
| W49 | Two tokens rated `high` on a depth nobody observed | E14 review of W42: fwARB and fwWETH (fwWETH labelled `alive`) are `high` with driver `no_liquidity`, every gap reads "no source reported pool depth", and on contract signals only they are `unknown`. A `high` built on an unobserved critical dimension is the E11 failure; it may instead be the no-trace escalation working as designed | `assess` replayed offline on their cached bodies; whether any pool listing was answered; rule for which escalation fired read from `_finalize`; fixed red-first only if the high rests on an unobserved dimension | Open |
| W50 | "Liquidity signals stripped" names the wrong thing, and USDT/WBTC are matched by symbol | E14 review of W42: the ablated column keeps only contract categories (`bench/run_benchmark.py` CONTRACT_CATEGORIES), so it also strips pool age, lifecycle and impersonation -- "contract signals only" is accurate, and /method, `EXPERIMENT_C.md` and the landing page say "liquidity signals". `_named_centralised` matches the symbol alone, so a lookalike "USDT" row would publish "rated low (4 of 5)" | Every surface grepped and renamed with its guard; `_named_centralised` matched on the canonical addresses; `tests/test_published_numbers.py` green | Open |

## Yours

| # | Item | Why it matters | Verify | State |
|---|---|---|---|---|
| W5 | A second, independent sell-simulation source | **Prerequisite for W3, not just a coverage fix.** The adversarial cohort cannot be cleaned without a third oracle the engine does not read: requiring honeypot.is corroboration would make the label circular under B1/B2. 15 of 17 `unsafe` tokens hold under $1, so the recall figure is measured on a cohort selected by recency rather than hostility. Also reduces the unknown rate | **Blocked** on a credential, not on engineering. Probed 2026-09-06: staysafu unreachable (SSL), quickintel 401, tokensniffer 401, de.fi public endpoint 404. Every candidate needed a paid key (**2026-09-15, from its pricing page:** Quick Intel now lists a free API Testing tier, 200 calls a month on approval, and a keyless pay-per-scan endpoint at $0.03 paid in USDC; whether its scan simulates a sell is not yet known; next owner step: apply for the testing tier) — this is a W9-shaped item that belongs to whoever holds the budget | Blocked |
| W9 | `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` as GitHub Secrets | One action unlocks two things: automatic deploys, and the external-caller line on the scorecard, currently `not measured` and worth 5 points that cannot be earned any other way | `deploy.yml` stops skipping; `python bench/usage.py` returns a real answer | Done R17 |
| W10 | Create the two accounts the remaining directories need | **Everything submittable without an account of Jake's is submitted (2026-09-08).** Live: official registry, PulseMCP, Glama, Smithery. Queued: mcp.so, mcpservers.org, awesome-mcp-servers (their bot's Glama check answered), awesome-remote-mcp-servers, Docker MCP registry, Cline marketplace. What was left is two signups, and **the owner parked both on 2026-09-08**: a free Console account for the Claude plugin directory, and a verified identity on the OpenAI platform. Neither is abandoned work — `plugin/` validates clean against the review pipeline's own check and `docs/OPENAI_SUBMISSION.md` has every field written with its test cases already run against production, so both are one signup away whenever that changes. They stay in the `CHANNELS` denominator: a row leaves it when it has been *measured* worthless, never because we chose not to try | `CHANNELS` in `bench/scorecard.py`, updated only by someone who went and looked | Parked |
| W11 | Answer the 2026-09-18 gate | The bar is `gate_verdict()` in `bench/usage.py`, frozen 2026-09-07 and printed by every run -- do not restate it here, this cell said "≥1 external caller" for a day after that rule was superseded. The first gate that can stop anything | `python bench/usage.py`, counting rule already fixed in code; then a `Resolved:` line in `STRATEGY.md` §8, which `test_gates_get_reviewed.py` requires once due | Open |
| W12 | Decide the price-history trade-off | `HANDOFF.md` §9.1. Reading OHLCV buys drawdown detection and voids the outcome column as an independent measurement. Three routes written up; none chosen | A decision recorded in `DECISIONS.md`, either way | Open |
| W25 | Decide whether the snapshot archive is a moat or a public good | `STRATEGY.md` §4.1 called it "the only part of this product that cannot be copied" while `snapshot.yml` committed it to the **public** repo four times a day. The exclusivity half was already false; "cannot be backfilled" survives | **Decided 2026-09-09, DECISIONS B15: keep publishing.** Measured first -- `.git` is 5.8 MB packed after 6 days, 0.95 MB/day, 200 MB in ~205 days -- so the cost is known and dated rather than feared. Publishing buys reproducibility, which a hostile review had just shown we did not have, and R2 would have made W7 unverifiable by an outsider. The move to R2 is not cancelled, it is **triggered**: at 200 MB packed, days older than 90 go to R2 and git keeps a rolling window. `tests/test_archive_budget.py` fails when that day arrives | Done R19 |
| W13 | Move the Cloudflare account and zone ids out of the public repo | Neither is a secret and neither can be rotated, so this was never credential hygiene: combined with the maintainer's name, email and the workers.dev subdomain, they are what makes a “Cloudflare security team” phishing message sound like it already has access | **Done 2026-09-09.** Out of `HANDOFF.md`, into `../vetagent-private-notes.md` outside the repo, and `tests/test_no_private_identifiers.py` fails the build if either returns — by hashing what it finds, so the guard does not contain the value it forbids. The original acceptance test was insufficient and is corrected here: **both values remain in public commit `5a16721` and cannot be un-published.** Removing them from HEAD lowers casual discoverability and nothing more; rewriting history would break every clone and every SHA pinned in `tools/rounds.py` to buy obscurity that was never the control. The real controls are that the API token has never been committed (`.wrangler/` gitignored and untracked, verified) and 2FA | Done R19 |
| W29 | Get three free market-data API keys, so a two-hour probe can settle whether a key ends the `unknown` answers | **Measured 2026-09-14, not inferred:** production answers `unknown` with the gap reason `upstream request failed (dexscreener 429, geckoterminal 429)` -- both free upstreams throttle the Worker's shared egress IP. CoinGecko's own docs say keyless access is "IP-based rate limiting -- shared across all users on the same IP", while a free Demo key gets 100 calls/min; DexScreener has no key or paid tier at all, so paying cannot fix its half. **Do not pay yet** (research, 2026-09-14): that a key helps is documentation, not a measurement from a Worker. The keys: a CoinGecko Demo key ($0, drop-in for today's GeckoTerminal fallback, has distinct sellers), a DexPaprika free key ($0, has both reserve amounts), a Codex "almost free" key ($1 verification, the only one with reserves and unique sellers in one query; next tier $350/mo). I build the probe Worker once the keys exist | Decision rule fixed before running: over 12 rounds in 2 hours from a throwaway Worker, **pass** if in rounds where the keyless control drew a 429 the keyed calls drew none and at most 1% non-2xx; **inconclusive** if the control never drew a 429 (re-run when production fails); **fail** for a provider answering 429/403/1020 under its documented limit. Pay only if a provider passes and projected volume exceeds its free allowance; the smallest paid step is CoinGecko Basic at $35 for one month. **Ran 2026-09-14 14:00-15:52 UTC, 12 rounds, all four providers PASS.** Keys set by the owner as secrets on a throwaway Worker (vetagent-keyprobe, not in this repo). In all 12 rounds a keyless control drew a 429: GeckoTerminal 48 of 60 calls, DexPaprika 38 x 429 and 22 x 402 (its per-IP monthly allowance already spent by the shared egress). In the same rounds from the same Worker, keyed calls: CoinGecko 60/60 HTTP 200, DexPaprika 60/60 plus 60/60 pool details, Codex 60/60 -- zero 429s, zero non-2xx. Field check passed on every chain in every round for all four; the only misses were DAI's top pool on CoinGecko (no seller count) and DexPaprika detail (reserves), 3 rounds each. Median latency: Codex 186 ms, CoinGecko 401 ms, DexPaprika 844 ms. **What this does not show:** every round ran from one data centre (KIX) and one egress IP, and keyless DexScreener answered 200 on all 60 calls here although production logged `dexscreener 429` the same day -- so it settles that a key escapes the throttling, not which colos production is throttled in. Two drained tokens were used instead of three, stated in the probe before it ran. **Next, owner's call:** which provider VetAgent adopts, and whether its free-tier terms (CoinGecko Demo: attribution required; Codex: 'personal & hobby') allow a public free service. **Owner, 2026-09-15: terms are fine, integrate.** Codex was then rejected on its data, not its terms: real response bodies captured by the probe Worker show its pool listing ranking testnet pools and int64-max liquidity first for WETH, and on mainnet a "USDT pool" holding 30,250,000,000 USDT -- fed to the depth check (E21) that would reopen the fabricated-depth hole (`bench/production/codex-samples-2026-09-15.json`). CoinGecko, the same data VetAgent already falls back to, is integrated instead (D8): DexScreener, then CoinGecko with the key, then keyless GeckoTerminal, and the contested-honeypot seller count goes through the key too. The probe Worker and its KV namespace are deleted. **Left for the owner:** set `CG_DEMO_KEY` on the vetagent Worker -- until then the code runs keyless, exactly as before. Verify: `bench/production/verdicts.json`'s unknown share falls in the days after the secret is set, from 40.7% on 2026-09-14 **Secret set by the owner 2026-09-15**; a manual probe of USDC on Base the same day answered with `liquidity_source: coingecko`. The unknown-rate check waits on W35 and W36 | Done R22 |
| W44 | Decide what a honeypot flag backed by failed holder sells is worth once the chain shows sells | The 2026-09-15 audit proposed releasing these flags; checked, that release was W16 (rejected R9). Its addendum (same day) then measured the instrument behind the flag, and the measurement holds on the engine's own captured answers: of 298 unflagged answers with a holder test, **none** has 4 or more failed holders and the highest share is 4.3% (PHA, 2 of 47); 13 of the 54 flags with a passing simulation have a share under 1% (STAR 4 of 2,458, GALE 8 of 4,958). So the flag behaves like a count trigger -- about 4 failures in any sample -- and a low share on thousands of tested real holders is background, not the blacklist signature; a high share is (XPL 530 of 3,647, SYP 23 of 224). Reading the share is not W16: the holder test is sells simulated from real holders, not other people's trades. **Options:** keep today's rule (30 false blocks); the addendum's graded rule -- Wilson 95% bound on failed/tested, n under 20 warn, lower bound at 20% or more fatal, lower bound under 5% plus the existing chain check released (16 of the 20 honeypot false blocks released, SYP high -> medium); or the same rule with the release gated on the **upper** bound under 5%. **My recommendation, changed by this measurement from "keep until W40":** the upper-bound form. Releasing is the silencing direction (E17), so it should need confidence the share is low, not the absence of confidence it is high: the lower-bound form releases XUSD (2 of 39, upper bound 16.9%), KITE (3 of 53, 15.4%) and ZEC (45 of 845, 7.1%). Upper bound under 5% releases 11 of the 20 (AGT, AKE, BIO, BITCOIN, LIGHT, SKI, SPX, STAR, TRUST, mubarak, quq), keeps Beat (dead) at warn, still releases GALE (dead, 8 of 4,958), needs no separate n floor, and anchors the 5% on the measured unflagged ceiling. Also: the 20% fatal band is not "unchanged" -- today a chain showing sells downgrades any flag, and that band would override it; it has to be pre-registered as the tightening it is. Arithmetic on the captured answers only, not a benchmark run (scratchpad `w44_check.py`) | A decision recorded in `DECISIONS.md` with the cuts written before one `python bench/run_benchmark.py` re-run, after the 2026-09-18 gate, then left alone until W40's cohort exists | Open |

## Closed

| # | Item | Outcome |
|---|---|---|
| W19 | Measure whether the pool we pick is the one that is wrong | **Done R15.** Ran the count O4 was gated on. 'Some pool disagrees' fires on 30.9% of tokens and is dust; 'the pool we picked disagrees with its peers' fires on 0.30% at 100x and is a real defect. Shipped as a selection rule in `_pick_best`. It was hiding a live P0: UNI priced at $4,576,980 by a pool with two trades in it |
| W20 | Compress the snapshot archive before it outgrows the repo | **Rejected 2026-09-07, corrected 2026-09-08. The verdict stands; three of its numbers did not, and the review caught them.** The original ticket projected ~250 MB/yr of files and ~1 GB of history, read off `du -sh .git` and the *loose* object store -- a transient local artefact, since git had not packed and GitHub packs on receive. Packed, the whole repository is 4.10 MiB and 9.33 MB of raw snapshot blobs collapse to 807 KB. **But the rejection then made the same class of error in the other direction.** (a) Its ~56 MiB/yr straddled three ramp-up days; measured over 2026-09-05 and 09-06, the only complete four-pass days, steady state is **92 MiB/yr packed** -- 11 years to GitHub's 1 GB soft limit, not 18. (b) It measured the history half only. The **working tree** grows at **~486 MB/yr** (1.40 MB/day over the same two days), and that is the number every CI checkout and every clone actually pays, five times the packed rate. (c) It argued gzip was wrong on readability, which is weak; the real reason is that gzip makes it **worse**. Measured: git stores all four versions of the 1,690,658-byte 09-05 file in 322,470 packed bytes, while `gzip -9` of the final version alone is 328,023 -- a .gz blob cannot be deltified and the plain blobs never leave history, so gzipping closed days is strictly additive. Nothing to do. Re-open on the constraint that actually binds: `du -sh bench/snapshots` above ~1 GB (about two years out), or a CI checkout over ~2 minutes. |
| W14 | LP lock / burn detection | **Rejected** in R9. Measured over 74 V2 pairs: "LP is fully pullable" catches 4 of 8 bad tokens and fires on 22 of 38 good ones — worse than chance, because most honest projects never burn LP. Removed from the scorecard denominator rather than left as a permanent gap |
| W15 | Chain-history backfill via reserve events | **Rejected** in R8. A genuinely independent class of evidence, but free RPCs cap `eth_getLogs` at 10k–50k blocks regardless of filter, so one pool-year is ~1,580 calls. Swap-log harvesting was built instead |
| W1 | Detect dormant owner powers from bytecode | **Reopened 2026-09-07.** The disclosure ships and is correct. What was closed and should not have been is the R12 conclusion that the powers do not discriminate: no measurement script was ever committed, no bytecode was cached, and the published table cannot be reproduced from the shipped selector list. A negative result used to justify not building something, that nobody could re-run | **Done R19, and the acceptance test had already been met before anyone checked** -- which is its own small lesson. This row asked that `python bench/owner_powers_measure.py` reproduce a per-power recall table from cached bytecode and that the list reach 50% on at least one power. The script is committed, the 559-contract cache is committed and tracked (7.4 MB, `bench/cache_bytecode` deliberately not gitignored), it runs offline, and mint was at 51.9% on 2026-09-09 before W18 touched anything. Closed on the criterion as written, not on a fresh one. After W18 the table reads 89.5 / 78.9 / 55.1 / 52.6 and is reproducible two ways -- this script drives `risk._powers_from_code` end to end, `bench/selector_mine.py` intersects sets, and they agree | Done R19 |
| W16 | Let completed on-chain sells settle sellability | **Rejected** in R9. 72 of 97 unknowns had 20+ sells and 0 were bad, including WETH — but a simulation tests whether *you* can sell and a completed trade shows *someone else* could, and a blacklist honeypot is built to make those look identical. Reported as context, gap kept open |
| W45 | Cache honeypot.is answers for hours to cut production `unknown` | **Rejected 2026-09-15**, from the numbers audit. `_STALE_OK_SECONDS` is also the edge lifetime of every upstream, so hours-old prices would feed the depth and drain checks; E22 caps confidence, never the verdict, so an hours-old "sellable" would still read `low` against P8; and nothing recorded says honeypot.is causes the production unknowns -- the only named production cause is the liquidity side. Revisit with W35's data, per host, for old pairs only, never producing `low`. |
